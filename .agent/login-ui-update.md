# Login UI Update (Architect HRM)

## Goal
Update the `LoginPage` component to match the "Architect HRM" Stitch design.

## Tasks Completed
- [x] Extracted design tokens and structure from Stitch HTML output.
- [x] Replaced the old dark/green themed login page with the new Light mode design.
- [x] Implemented "Glassmorphism" login card and left-side typography.
- [x] Swapped `material-symbols-outlined` for native `lucide-react` icons (Layers, Mail, Eye, etc.) to match the project's tech stack.
- [x] Re-integrated the `useAuth` login flow, error banners (`Alert` component), and mock account seeds seamlessly into the new UI layout.

## Next Steps
- Verify the UI on mobile screens (responsive adjustments if needed).
- Proceed to update the Dashboard or other pages as requested by the user.
